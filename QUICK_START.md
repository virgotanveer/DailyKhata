# Daily Cash Flow Tracker - Quick Start Guide

## 🚀 1-Click Startup Instructions

### For Windows Users:
1. **Double-click** the `start-cash-tracker.bat` file
2. Wait for the server to start (automatically installs dependencies if needed)
3. Your browser will open automatically to `http://localhost:3000`
4. Press Ctrl+C in the terminal to stop the server

### For Mac/Linux Users:
1. **Double-click** the `start-cash-tracker.sh` file
   - If it doesn't run, right-click → Open With → Terminal
2. Wait for the server to start (automatically installs dependencies if needed)
3. Your browser will open automatically to `http://localhost:3000`
4. Press Ctrl+C in the terminal to stop the server

## ✨ What the Startup Script Does:

1. **Checks Node.js** - Ensures Node.js is installed
2. **Installs Dependencies** - Automatically installs required packages if missing
3. **Starts Server** - Launches the development server
4. **Opens Browser** - Automatically opens the application in your default browser

## 📋 System Requirements:
- **Node.js** (Download from https://nodejs.org/)
- **Modern Browser** (Chrome, Firefox, Safari, Edge)

## 🔧 Manual Startup (if scripts don't work):
1. Open terminal/command prompt in this folder
2. Run: `npm install` (first time only)
3. Run: `npm run dev`
4. Open browser to `http://localhost:3000`

## 📞 Support:
If you encounter any issues, make sure:
- Node.js is properly installed
- You're in the correct directory
- Internet connection is available (for dependency installation)

---
**Enjoy using your Daily Cash Flow Tracker!** 💰